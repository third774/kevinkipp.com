---
import { cn } from "../utils/cn";

interface Props extends astroHTML.JSX.HTMLAttributes {}
---

<div
	class={cn(
		"mx-auto w-[min(100%,var(--max-col-width,70ch))] flex-auto px-4",
		Astro.props.class,
	)}
>
	<slot />
</div>
